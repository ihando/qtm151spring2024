12: REPLACE, CUT ALSO ON HW THo
14: get col names, types. aggregate + groupby + query. merging
15: check col similar names, dictionary, renaming col with dict,
RENAME COLUMNS, MERGING concatenate datasets
16: